import React from 'react';

import { LabeledFormControl } from './LabeledFormControl';

export const LabeledTextArea = (props: any) => (
  <LabeledFormControl component="textarea"> </LabeledFormControl>
);
